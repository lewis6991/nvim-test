local uv = vim.uv or vim.loop
local cwd = uv and uv.cwd() or '.'

return {
  statsfile = cwd .. '/luacov.stats.out',
  reportfile = cwd .. '/luacov.report.out',
  include = {
    'lua/.*',
  },
  exclude = {
    'luacov/',
    '^test/',
    '^example/',
  },
  runreport = true,
}
